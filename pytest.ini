[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    login_guest: marker for guest tests
    login_user: marker for user tests
    need_review: marker for review my tests